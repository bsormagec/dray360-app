const mutations = {
  setOrders (state, orders) {
    state = orders
  }
}

const actions = {
  getOrders ({ commit }, params) {
    // api call
  }
}

export default {
  state: [],
  mutations,
  actions
}
