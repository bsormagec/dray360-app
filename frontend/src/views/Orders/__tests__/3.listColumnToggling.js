// import { fireEvent } from '@testing-library/vue'
// import Orders from '@/views/Orders/Orders'
// import store from '@/store/modules/orders'
// import renderAuthenticated from '@/utils/for_tests/render_authenticated'
// import endQeue from '@/utils/for_tests/end_qeue'

export default () =>
  describe('listColumnToggling', () => {
    it('hides a column', async () => {
      // const { container } = await renderAuthenticated(Orders, { store })
      // const dropdown = container.querySelector('.header__dropdown div[role="button"]')

      // fireEvent.click(dropdown)
      // await endQeue()

      // const menu = container.querySelector('.v-menu__content div[role="listbox"]')
      // const firstOption = menu.children[0]
      // fireEvent.click(firstOption)
      // await endQeue()

      // const columns = container.querySelectorAll('th[aria-label="Id"]')
      // console.log(columns)
    })
  })
